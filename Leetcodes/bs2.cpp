#include <bits/stdc++.h>
using namespace std;

int lower_bound(vector<int> &v, int element)
{
}

int main(int argc, char const *argv[])
{
    int n;
    cin >> n;
    vector<int> v(n);
    for (int i = 0; i < n; i++)
    {
        cin >> v[i];
    }
    return 0;
}
