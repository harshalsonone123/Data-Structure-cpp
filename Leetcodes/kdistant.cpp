#include <iostream>
using namespace std;

class Solution
{
public:
    vector<int> findDistantIndices(vector<int> &nums, in key, int k)
    {
        int N = A.size();
        vector<int> ans, idx;
        for (int i = 0; i < N; ++i)
        {
            if (A[i] == key)
            {
                /* code */
            }
        }
    }
};

int main(int argc, char const *argv[])
{

    return 0;
}
